type Point ={
    x:number;
    y:number;
    z?: number // optional property.
};

const myPoint: Point ={
    x:1,
    y:2,
    z:4 // we can assign z value or we can leave it x and y only its due to ?: operatror
    // tara 
    // z?: bata ? symbol hatayo vane  ra z ko value assign garenna vane error 
} 