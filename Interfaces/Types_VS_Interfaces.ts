// for not object type is used
type color = 'red'| 'blue';
// union of two string literals


/*
type person = {
    name: string,
};
type person ={ 
    age: number;
}

const Person:person ={
    name:'hari';
    age: 12;
    // here it read from only top person
   // so there is no age 
    // hence throw error
}

// interface: 
interface pperson {
    fname: string;
};
interface pperson {
    age: number;
};
const per:pperson = {
    fname: 'ram',
    age: 23
}
// here it intersects the from top and last pperson
*/ 