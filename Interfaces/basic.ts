// interfaces almost  same as purpose as type aliases
// to describe the shapes of object
 type P ={
    x: number,
    y: number
 };
 const pt: P = {x:23,y:32};

 // interfaces: 

 interface points {
    a: number;
    b: number;
 };
 const pts: points ={ a:10,b:45};