const nameList: Array<string> =[];
const nameList1: string[]=[];
// these two are identical  

const bools: Array<boolean> =[];


type point ={
    x?: number;
    y: number;
};
const coors: point[]=[];
coors.push({x:23,y:45});

console.log(coors);